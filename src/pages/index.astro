---
import Layout from '../layouts/Layout.astro'
import '../styles/global.css'
import { ChartApp } from '../components/ChartApp/ChartApp'
console.log('logged in terminal') // JS here is not logged in the browser as this is server-side rendered
---

<Layout title="Stock performance tracker">
  <main class="flex justify-center items-center w-full flex-col h-screen p-12">
    <div class="prose text-center">
      <h1 class="my-8">Stock performance tracker</h1>
    </div>
    <div class="w-full p-12 border border-gray flex-grow">
      <ChartApp client:only="react"/>
    </div>
  </main>
</Layout>
